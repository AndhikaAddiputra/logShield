# Mobile app — CouchDB replication target (include credentials in production via secure storage)
VITE_COUCHDB_URL=https://localhost:5984/logshield
VITE_COUCHDB_USER=
VITE_COUCHDB_PASSWORD=

# Server — CouchDB admin API (optional; mobile may sync directly to CouchDB)
COUCHDB_URL=http://localhost:5984
COUCHDB_USER=admin
COUCHDB_PASSWORD=

# Server — JWT secret (change in production!)
JWT_SECRET=logshield-dev-secret-change-in-production

# Web dashboard API
VITE_API_URL=http://localhost:4000
